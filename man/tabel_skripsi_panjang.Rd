% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabel.R
\name{tabel_skripsi_panjang}
\alias{tabel_skripsi_panjang}
\title{Tabel panjang lintas-halaman sesuai pedoman}
\usage{
tabel_skripsi_panjang(
  df,
  judul,
  label,
  sumber = NULL,
  ket = NULL,
  align = NULL,
  nomor_kolom = TRUE,
  lebar = NULL,
  regang_baris = 1.15,
  escape = TRUE,
  simpan = NULL,
  timpa = FALSE
)
}
\arguments{
\item{df}{Data untuk badan tabel (\code{data.frame}/\code{matrix}). Nama kolom dipakai
sebagai judul kolom.}

\item{judul}{Judul tabel (tanpa kata "Tabel N." dan tanpa titik akhir).}

\item{label}{Label LaTeX untuk rujuk silang, mis. \code{"tab:miskin"} (hindari
awalan \verb{tbl-}/\verb{fig-} agar tidak ditangani ulang oleh Quarto).}

\item{sumber}{Teks sumber data (tanpa kata "Sumber:"). \code{NULL} (default) untuk
data primer/olahan sendiri - baris sumber tidak ditampilkan.}

\item{ket}{Keterangan tambahan di bawah tabel (tanpa kata "Keterangan:").}

\item{align}{Vektor perataan kolom (\code{"l"},\code{"c"},\code{"r"}) sepanjang jumlah kolom.
\code{NULL} (default): kolom pertama kiri, sisanya kanan.}

\item{nomor_kolom}{Tampilkan baris nomor kolom \verb{(1)(2)...} (default \code{TRUE}).}

\item{lebar}{Lebar tabel sebagai pecahan/persen dari lebar teks. \code{NULL}
(default) memakai \strong{0,8 (80\%)}. Nilai \verb{< 0,8} dinaikkan otomatis ke 0,8
(minimum); nilai \verb{> 1} dianggap persen; maksimum 1 (lebar penuh).}

\item{regang_baris}{Faktor tinggi/kerapatan baris (\verb{\\\\arraystretch}). Default
\code{1.15}. Nilai lebih kecil (mis. \code{1.0}) membuat baris \strong{agak rapat}, nilai
lebih besar (mis. \code{1.4}) membuat baris \strong{agak longgar}.}

\item{escape}{Lolos-kan karakter khusus LaTeX (\verb{& \% # _}) pada judul kolom &
data. Default \code{TRUE}. Set \code{FALSE} bila Anda sengaja menulis LaTeX/math.}

\item{simpan}{Bila diisi path berkas (mis. \code{"tex/tabel-miskin.tex"}),
kode LaTeX ditulis ke berkas itu dan disisipkan via \code{\input}; mahasiswa
dapat mengedit berkas tersebut untuk penyesuaian manual. Default \code{NULL}
(kode dikembalikan inline seperti biasa).}

\item{timpa}{Bila \code{FALSE} (default) dan berkas \code{simpan} sudah ada,
berkas TIDAK ditimpa sehingga editan dipertahankan; \code{TRUE} membuat ulang.}
}
\value{
Objek \code{knitr::raw_latex} (PDF) atau \code{knitr::kable} (non-LaTeX).
}
\description{
Seperti \code{\link[=tabel_skripsi]{tabel_skripsi()}} tetapi memakai \code{longtable}/\code{xltabular} untuk tabel
yang melampaui satu halaman: judul kolom dan baris nomor kolom \strong{terulang} di
tiap halaman, disertai keterangan \strong{"Tabel N (lanjutan)"}. Bentuk tetap grid
(garis kolom) dengan huruf \verb{\\\\small} (>= 8 pt sesuai pedoman).
}
\details{
Berbeda dengan \code{\link[=tabel_skripsi]{tabel_skripsi()}}, \strong{lebar tabel panjang minimal 80\% lebar
teks} dengan batas kiri (rata margin) dan batas kanan terkontrol pada lebar
target, sehingga tepi tabel rapi dan seragam. Kolom label (perataan \code{"l"})
melebar untuk mengisi ruang, sedangkan kolom angka tetap ringkas.
}
